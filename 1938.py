# SWEA D1
import math
a, b=map(int,input().split())
result=a/b
print(a+b)
print(a-b)
print(a*b)
print(math.floor(result)) # 소수점 이하의 숫자는 버린다
# floor() 함수로 소수점 숫자를 내림
# 사용방법 import math 선언 후 math.floor() 함수 사용
